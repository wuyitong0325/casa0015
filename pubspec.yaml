name: mobile_app_flutter
description: BLE SGP30 dashboard with needle gauges
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_blue_plus: ^1.34.5
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  firebase_core: ^4.6.0
  cloud_firestore: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true